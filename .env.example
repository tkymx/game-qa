# Copy to ~/.config/game-qa/.env (shared by every clone/worktree and seen by the monitor app).
# A .env at the game-qa root or an exported variable also works.
# TypeSafe AI (Jev) API key — only needed for --provider=jev
# Get one at https://console.typesafe.ai/
TYPESAFE_API_KEY=your-api-key
